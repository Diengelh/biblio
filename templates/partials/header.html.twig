<!-- Navigation-->
<nav class="navbar navbar-expand-lg navbar-dark bg-dark fixed-top" id="mainNav">
    <div class="container px-4">
        <a class="navbar-brand" href="{{path('app_main_index')}}" title="revenir à l'accueil">Biblios</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarResponsive" aria-controls="navbarResponsive" aria-expanded="false" aria-label="Toggle navigation"><span class="navbar-toggler-icon"></span></button>
        <div class="collapse navbar-collapse" id="navbarResponsive">
            <ul class="navbar-nav ms-auto">
                <li class="nav-item"><a class="nav-link" href="{{path('app_book')}}">Catalogue</a></li>
                {% if app.current_route == "app_main_index" %}
                    <li class="nav-item"><a class="nav-link" href="#contact">Contact</a></li>
                {% endif %}
                {% if not app.user %}
                 <li class="nav-item"><a class="nav-link" href="{{path('app_login')}}">Se connecter</a></li>
                {% else %}
                 <li class="nav-item"><a class="nav-link" href="{{path('app_shop_index')}}">Panier</a></li>
                 <li class="nav-item"><a class="nav-link" href="{{path('app_borrowing_index')}}">Mes emprunts</a></li>
                 <li class="nav-item dropdown">
                    <button class="btn btn-dark dropdown-toggle" data-bs-toggle="dropdown" aria-expanded="false">
                        Extras
                    </button>
                    <ul class="dropdown-menu dropdown-menu-dark">
                        <li><a class="dropdown-item" href="#">Mon profil</a></li>
                        <li><a class="dropdown-item" href="{{path('app_password')}}">Changer de Mot de passe</a></li>
                        <li><a class="dropdown-item" href="#">Résilier Mon compte</a></li>
                         {% if app.user.isAdmin %}
                            <li><a class="dropdown-item" href="{{path('admin')}}">L'administration</a></li>
                         {% endif %}
                        <li><a class="dropdown-item" href="{{path('app_logout')}}">Déconnexion</a></li>
                        
                    </ul>
                </li>
                {% endif %}
                
            </ul>
        </div>
    </div>
</nav>
